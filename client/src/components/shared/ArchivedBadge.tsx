/**
 * Subtle pill indicating a listing is archived.
 */
import React from 'react';

const ArchivedBadge = () => (
  <span className="text-xs px-2 py-0.5 rounded-full bg-[var(--yr-panel-muted)] text-gray-500 font-medium">
    Archived
  </span>
);

export default ArchivedBadge;
