export type { IconProps } from './icons';

/**
 * Kept as a module so existing imports keep working; the glyph itself lives in
 * the icon set with every other one.
 */
export { ArrowRightIcon as default } from './icons';
