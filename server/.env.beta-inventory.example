# Copy only the value into the external operator-owned profile file
# <absolute-profile-directory>/beta-inventory.env.
# Do not create the real profile inside a repository worktree.
# Provision an Atlas database user with the read role scoped only to Beta.
MONGODBURL=<mongodb-srv-url-for-read-only-beta-user>
