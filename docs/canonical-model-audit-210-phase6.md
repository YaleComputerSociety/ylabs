# Canonical model audits and validator tightening (#210 Phase 6, #727)

This report documents the reproducible audits added for #210 Phase 6 and their findings on the Development database.
Every finding here is regenerated by a committed, read-only script; rerun the script to refresh the numbers.
Counts below were captured against the `Development` database.

## 1. Dual-write verification

Script: `yarn --cwd server model-refactor:legacy-writer-scan --environment development`.
It statically scans runtime source (operator scripts under `server/src/scripts/` are excluded because the scope is runtime writers), confirms the retired Mongoose models are unregistered, and confirms the retired collections carry no data.

Clean surfaces:

- Retired collections `research_groups`, `research_group_members`, `papers`, `paper_authors` hold zero documents; `research_groups` and `research_group_members` no longer exist at all.
- Retired Mongoose models `ResearchGroup`, `ResearchGroupMember`, `Paper`, `PaperAuthor` are not registered.
- Zero runtime writers set the retired access booleans (`acceptingUndergrads`, `openness`, `acceptanceConfidence`, `opennessSignals`) removed by #463.
- Zero runtime writers set the retired professor-profile bibliographic fields on `User`, and no raw driver access targets a retired collection.

Residue found (tracked as #755, not fixed here because it is a behavioral change to live services and the scraper materializer):

- The legacy ownership field `researchGroupId` is still dual-written alongside `researchEntityId` at `services/listingService.ts` (listing create) and `scrapers/entityMaterializer.ts` (member upserts), and is re-emitted in the legacy-shaped read DTO at `services/researchEntityMembershipAccessor.ts`.

## 2. Reference, orphan, and visibility integrity

Script: `yarn --cwd server model-refactor:reference-integrity --environment development --include-samples`.
It counts dangling and missing-required references on the canonical relationship edges, reusing the reference-audit primitives from `betaDataQualityCore.ts`.

Result: clean.
All 11 audited edges reported zero missing-required and zero orphaned references, including the edges named in the issue: `RoleAssignment.personId` and `RoleAssignment.target.id` (both `RESEARCH_ENTITY` and `ORG_UNIT` targets), `Signal.researchEntityId` and `Signal.source.evidenceIds`, and both `ResearchEntityRelationship` endpoints.

## 3. Validator tightening

Script: `yarn --cwd server model-refactor:strict-readiness --environment development`.
It counts documents that already fail each canonical collection's desired `$jsonSchema`.

Strict-ready (flipped to `validationLevel: 'strict'` in the registry): `accounts`, `evidence_claims`, `org_units`, `research_plans`, `review_decisions`, `source_documents` (all zero non-conforming documents, and reference-clean).
Not clean, left at `moderate` (tracked as #753): `researchers` (6 non-conforming of 17,409), `role_assignments` (1 of 4,971), `taxonomy_terms` (15 of 5,306).

The strict flips are encoded in `canonicalMongoValidatorRegistry.ts`, and a reviewed Development dry-run confirms the plan is exactly six `strict` collMods plus three `moderate` collMods.
The Development database currently carries no canonical validators, and the least-privilege application credentials cannot run `collMod`, so applying the plan to Development is tracked as #752 (it needs admin credentials and the runbook's dry-run and apply flow).
Carrying any verified-clean flip forward to Beta or Production is tracked as #754, a separate live-database change that needs its own review.
