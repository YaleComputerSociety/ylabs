# Canonical model audits and validator tightening (#210 Phase 6, #727)

This report documents the reproducible audits added for #210 Phase 6 and their findings on the Development database.
Every finding here is regenerated by a committed, read-only script; rerun the script to refresh the numbers.
Counts below were captured against the `Development` database on 2026-09-22.

The collection list is not a constant.
The canonical registry is whatever `canonicalMongoValidatorRegistry.ts` declares today, and retiring a model removes its collection from every plan in this document: #2814 retired the frozen evidence claim-graph, which is why `evidence_claims`, `review_decisions`, and `source_documents` are no longer audited or planned here and no longer exist on Development.
Read the collection set and the plan fingerprint from a fresh dry-run artifact rather than from this file.

## 1. Dual-write verification

Script: `yarn --cwd server model-refactor:legacy-writer-scan --environment development`.
It statically scans runtime source (operator scripts under `server/src/scripts/` are excluded because the scope is runtime writers), confirms the retired Mongoose models are unregistered, and confirms the retired collections carry no data.

Clean surfaces:

- Retired collections `research_groups`, `research_group_members`, `papers`, `paper_authors` no longer exist on Development, so they hold no documents.
Absence on Development says nothing about Beta or Production, which are reached by copy rather than by this repair.
- Retired Mongoose models `ResearchGroup`, `ResearchGroupMember`, `Paper`, `PaperAuthor` are not registered.
- Zero runtime writers set the retired professor-profile bibliographic fields on `User`, and no raw driver access targets a retired collection.
- The legacy ownership field `researchGroupId` is no longer dual-written anywhere: the `legacyOwnershipFieldKey` rule reports zero findings now that #755 has landed.

Residue found, one finding, still open:

- `utils/servedFieldContributionLabels.ts` maps the retired access boolean `acceptingUndergrads` to a student-facing "Undergrad access" credit.
The scan's rule name says write, but this is a read: the label map is keyed by stored `fieldProvenance` field names, so a schema field that #463 deleted keeps a reader for as long as the provenance key naming it survives.
Retiring the `acceptingUndergrads` lane end to end is tracked as #2055, which owns this file; do not strip the label separately.

### A count of stale keys does not tell you the field is dead

Two readings an hour apart on 2026-09-22 disagreed.
`fieldProvenance.acceptingUndergrads` read 837 research entities on the first, and zero on the second, while #2055's retirement work was in flight.
Nothing about the first reading marked it as about to change, and nothing about the second distinguishes a field that was never written from one a peer cleared minutes earlier.
So treat a zero as one observation with a timestamp, and confirm the writer is gone from the code before concluding a field is retired: the `observations` rows for that field did not move, staying at 2689 across both readings, and the access materializer still consumes them.

`fieldProvenance.openness` is the counterexample that is genuinely stale and still consequential.
It sits on 532 research entities, 343 of them `student_ready`, for a field with no writer and no reader by name.
It is still read by position: the visibility gate's `hasLiveSourceCitation` counts every `fieldProvenance.*.sourceUrl` as a citation, so a retired field's provenance entry is a gate input, and removing one can move a row's `all_citations_dead` verdict.
Measured before proposing removal: of the 343, zero lose their last live citation when only that key is dropped, while a control that drops the whole provenance map flips 4, so the zero is a real result rather than a dead instrument.
`retire:stale-access-signal-fields` unsets the eight top-level fields only, which is why these provenance keys survived it.

## 2. Reference, orphan, and visibility integrity

Script: `yarn --cwd server model-refactor:reference-integrity --environment development --include-samples`.
It counts dangling and missing-required references on the canonical relationship edges, reusing the reference-audit primitives from `betaDataQualityCore.ts`.

Result: clean.
All 10 audited edges reported zero missing-required and zero orphaned references, including the edges named in the issue: `RoleAssignment.personId` and `RoleAssignment.target.id` (both `RESEARCH_ENTITY` and `ORG_UNIT` targets), `Signal.researchEntityId` and `Signal.source.evidenceIds`, and both `ResearchEntityRelationship` endpoints.
The edge count tracks the model, not this document: it read 11 while the evidence claim-graph was still modelled.

## 3. Validator tightening

Script: `yarn --cwd server model-refactor:strict-readiness --environment development`.
It counts documents that already fail each canonical collection's desired `$jsonSchema`.

Strict-ready (flipped to `validationLevel: 'strict'` in the registry): all six canonical collections the registry declares today - `accounts`, `org_units`, `research_plans`, `researchers`, `role_assignments`, `taxonomy_terms`, each with zero non-conforming documents and reference-clean.
`researchers`, `role_assignments`, and `taxonomy_terms` were the last three to drift (originally 6, 1, and 15 non-conforming). Under #753 the `researchers` stubs were repaired to conform (`users:repair-researcher-display-names` filled the missing required `displayName` on account-linked researchers from the legacy user's first and last name), `role_assignments` and `taxonomy_terms` reached zero non-conforming, and all three were flipped to `strict`.

The strict flips are encoded in `canonicalMongoValidatorRegistry.ts`, and a Development dry-run plans exactly six `strict` collMods, no `createCollection` and no `noop`.
Every collMod carries the same three drift reasons (`validator-drift`, `validation-level-drift`, `validation-action-drift`) because Development declares no validator on any of the six.
Do not quote a `planFingerprint` from this document: it binds the registry to the observed collection options, so retiring a model, editing a schema, or a partial apply all move it.
Regenerate the artifact and read the fingerprint from it.

The Development database currently carries no canonical validators, and the application credentials cannot run `collMod` (`connectionStatus` with `showPrivileges` reports `readWriteAnyDatabase` only, which does not grant the action), so applying the plan to Development is tracked as #752 and needs credentials that can.
Applying the same plan to Beta or Production is not scheduled work: see the promotion warning in the runbook for why a validator applied there does not survive the next whole-collection copy.
