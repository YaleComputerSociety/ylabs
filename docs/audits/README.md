# Audits

Durable audit summaries live here when they are still useful after a task ends. Keep bulky generated artifacts such as screenshots, raw Playwright traces, browser evidence JSON, and one-off local reports in ignored `audit/` or `.audit/` directories.
